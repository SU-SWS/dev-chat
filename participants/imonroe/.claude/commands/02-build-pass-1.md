We are now going to code the project.

IMPORTANT: This project deploys via GitHub Pages. All logic, state management, and interactivity must run in the browser only — no server-side code. Use `localStorage` for any persistent storage.

Use the `docs/implementation_plan.md` as your road map.  Use the Pencil MCP tool to read the designs in the `designs/` folder to inform the visual styles.  If we are using a UI toolkit in the project, rely on those components to make the interactive components or visual elements whenever practicable.

Build the project.  Write tests as you go and use `prettier` or another linter to enforce code quality. Ensure tests pass correctly. If it makes sense, you can parallelize this work.

This step will be complete when you have fully implemented the entire project. As long as there are un-implemented features in the implementation plan, you aren't done.

Once you have the full project implemented, commit the changes to the current branch, then let me know this step is complete and wait for my go-ahead before continuing. When I give the go-ahead, if the context window is growing large, run `/compact` first — then follow the instructions in @.claude/commands/03-review-pass.md

