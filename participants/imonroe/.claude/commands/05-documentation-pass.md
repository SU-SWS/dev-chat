We are now going to perform a documentation pass over the project.

Take a look at the status of the codebase, and generate complete documentation.

This includes, but is not necessarily limited to: 
- a README.md with the project overview
- developer-facing documentation in the `docs/` directory
- user-facing documentation in the `docs/` directory.  This should be easy to follow, non-technical information for how to set up and use the project.

Make sure all the documentation reflects the actual state of the code.

After you have completed the documentation, commit the changes to the current branch, and then alert me that we have finished the project.



